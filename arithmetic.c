#include <stdio.h>

int main()
{
    // arithmetic operators

    // + (addition)
    // - (subraction)
    // * (multiplication)
    // % (modulus)
    // ++ (increment)
    // -- (decrement)

    int x = 5;
    int y = 2;

    int z = x + y;
    // int z = x / y;
    // int z = x +(float) y; this is how you manipulate to see the exact result of division

    // increment and decrement
    x++;
    y--;
    printf("%d\n", x);
    printf("%d", y);

    return 0;
}
